import { QualificationSchema } from './types';

export async function humanFeedback(
  research: string,
  email: string,
  qualification: QualificationSchema
) {
  /**
   * Send the research and qualification to the human for approval in slack
   */
}
